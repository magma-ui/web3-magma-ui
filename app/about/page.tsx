import { title } from "@/components/primitives";

export default function AboutPage() {
  return (
    <div>
      <h1 className={title()}>About</h1>
    <p>About page sounds about right since yk , we need to tell them about things and show maybe ? ......</p>
    </div>
  );
}
