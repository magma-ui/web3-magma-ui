import { title } from "@/components/primitives";

export default function PricingPage() {
  return (
    <div>
      <h1 className={title()}>Pricing</h1>
      <p>We need to figure out pricing and how to do that </p>
      <p>not just web3 payments but issue is normal bank transactions .[INTERNATIONAL]</p>
    </div>
  );
}
